{
  "name": "test-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bunx conflux build",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "dev": "bunx conflux dev",
    "lint": "eslint src",
    "start": "bunx conflux start"
  },
  "dependencies": {
    "@confluxjs/conflux": "workspace:*",
    "@fluxerjs/core": "catalog:"
  },
  "devDependencies": {
    "@confluxjs/tsconfig": "workspace:*",
    "@types/bun": "latest",
    "typescript": "catalog:build"
  }
}
